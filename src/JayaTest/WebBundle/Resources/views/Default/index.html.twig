{% extends "JayaTestWebBundle::base.html.twig" %}
{% block content %}
<h2>Гостевая книга</h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% for item in items %}
    [{{ item.createdAt | date("d.m.Y H:i:s") }}] <strong>{{ item.username }}</strong>: {{ item.message }}<br/>
{% endfor %}

{% endblock %}
